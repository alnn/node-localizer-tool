'use strict';

//const a = require('./actions');
var SAVE_SECTION = require('./constants').SAVE_SECTION;

module.exports = function (state, action) {

  state = state || { sections: [] };

  switch (action.type) {
    default:
      return state;
  }
};